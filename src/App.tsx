import './App.css'
import MainPage from './components/MainPage'

function App() {
  // App is a simple shell — routing happens in `main.tsx` to avoid nested Routers
  return <MainPage />
}

export default App
